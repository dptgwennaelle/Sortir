<nav>
    <ul>
         {% if app.user %}
            {% if app.user.administrateur %}
                <li><a href="{{ path('admin_villeliste') }}" title="Villes">Villes</a></li>
                <li><a href="{{ path('sortir_home') }}" title="Campus"></a>Campus</li>
            {% endif %}
            <li><a href="{{ path('sortir_liste') }}" title="Page d'accueil">Accueil</a></li>
            <li><a href="{{ path('profil_liste') }}" title="Page du profil utilisateur">Mon profil</a></li>
            <li><a href="{{ path('app_logout') }}" title="Page de déconnexion">Se déconnecter</a></li>
        {% endif %}
    </ul>
</nav>