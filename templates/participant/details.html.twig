{% extends 'base.html.twig' %}

{% block body %}
    <div>
        <img src="{{  asset('img/posters/profil/' ~participant.poster) }}" alt="{{ participant.nom}}">
    </div>

    <div class="participant-details-infos">
      <h1>Pseudo: {{ participant.identifiant }}</h1>
        <div>Prénom: {{ participant.prenom}}</div>
        <div>Nom: {{ participant.nom }}</div>
        <div>Téléphone: {{  participant.telephone }}</div>
        <div>Email: {{  participant.mail }}</div>
        <div>Campus: {{  campus.nom }}</div> // A revoir (clé étrangère)

    </div>


{% endblock %}

{% block title %}
     {{ participant.nom}}

{% endblock %}

{#
{% block extrastyles %}
    <style>
        body{
            background-image: linear-gradient(rgba(0, 0, 0, 0.9), rgba(0, 0, 0, 0.7)), url("{{ asset('img/backdrops/'~serie.backdrop) }}");
            background-size: cover;
        }
    </style>
{% endblock %} #}
