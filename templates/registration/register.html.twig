{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}

    <h1>Mon profil</h1>

    {{ form_errors(registrationForm) }}

    {{ form_start(registrationForm) }}

        {{ form_row(registrationForm.identifiant ,{label: 'Pseudo : '}) }}
        {{ form_row(registrationForm.prenom ,{label: 'Prénom : '}) }}
        {{ form_row(registrationForm.nom ,{label: 'Nom : '}) }}
        {{ form_row(registrationForm.telephone ,{label: 'Téléphone : '}) }}
        {{ form_row(registrationForm.mail ,{label: 'Email : '}) }}
        {{ form_row(registrationForm.plainPassword ,{label: 'Mot de passe : '}) }}

       {#  {{ form_row(registrationForm.campus ,{label: 'Campus '})

        doit encore ajouter: confirmation de mot de passe, photo, campus}}

       #}




    <button type="submit" class="btn">Enregistrer</button>
    <button type="submit" class="btn">Annuler</button> {# Revenir pour le fonctionnement #}
    {{ form_end(registrationForm) }}
{% endblock %}
